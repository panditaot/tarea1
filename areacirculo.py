import math

def areacirculo(radio):
    areacirculo = (math.pi)*(radio**2)
    return print("El área del circulo es {} unidades cuadradas".format(areacirculo))
radio=float(input("Teclea el radio para calcular el área de un círculo: "))
areacirculo(radio)

    
    
